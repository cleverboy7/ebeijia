package com.ebeijia.controller.action.test
import java.util.Map
import javax.jms.Destination
import javax.servlet.http.HttpServletRequest
import javax.servlet.http.HttpSession
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.beans.factory.annotation.Qualifier
import org.springframework.stereotype.Controller
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.ResponseBody
/**
 * TestAction
 * @author zhicheng.xu
 *         15/8/17
 */

@Controller class TestAction {
//  @Autowired
//  private val producerService: ProducerService = null
//  @Autowired
//  @Qualifier("queueDestination")
//  private val destination: Destination = null
//  @Autowired
//  @Qualifier("sessionAwareQueue")
//  private val sessionAwareQueue: Destination = null
//
//  @RequestMapping(value = Array("test.html"))
//  @ResponseBody def add(session: HttpSession, request: HttpServletRequest): Map[String, AnyRef] = {
//    producerService.sendMessage(sessionAwareQueue, "测试SessionAwareMessageListener")
//    null
//  }
}
